
data:
  author: F:\-- All Projects --\Pustak\test-authors.txt
  works: F:\-- All Projects --\Pustak\test-works.txt

spring:
  data:
    cassandra:
      keyspace-name: main
      username: <USERNAME>
      password: <PASSWORD>
      schema-action: create-if-not-exists 
      request:
        timeout: 10s
      connection:
        connect-timeout: 10s
        init-query-timeout: 10s

datastax:
  astra:
    secure-connect-bundle: secure-connect-pustak.zip
    db:
      id: <id>
      region: asia-south1
      keyspace: main
      applicatio:
